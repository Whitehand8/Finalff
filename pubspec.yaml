name: trpg_frontend
description: "TRPG Flutter Application"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.0
  provider: ^6.0.0
  socket_io_client: ^3.1.2
  cached_network_image: ^3.3.1
  http_interceptor: ^2.0.0
  go_router: ^16.2.5
  flutter_secure_storage: ^9.2.4
  dio: ^5.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true